{
  "project": "Packaged Food Inventory & Expiry Tracker (PWA)",
  "plan_file": "C:\\Users\\manav\\.claude\\plans\\development-roadmap-atomic-fountain.md",
  "build_order": [
    "0 Setup", "1 DB Schema", "2 Auth & Onboarding", "3 Manual Entry",
    "4 Inventory List", "5 Voice Input", "6 OCR Scanning", "7 Verify Screen",
    "8 Notifications", "9 Rewards", "10 PWA Polish", "11 E2E Testing", "12 Future stub"
  ],
  "current_module": "4 Inventory List",
  "workflow_agreements": [
    "Pause after each module's Done-when checklist for manual review/testing before starting the next module",
    "Lazy-load heavy WASM deps (OpenCV.js, vosk-browser, Tesseract.js) via dynamic import from Module 0 onward",
    "Model per module: see model_guide below (both run at medium reasoning effort)",
    "Update this file (and PROGRESS.md) after every meaningful change, not just at module end"
  ],
  "model_guide": {
    "0": "Sonnet 5", "1": "Opus 4.8", "2": "Sonnet 5", "3": "Sonnet 5",
    "4": "Sonnet 5", "5": "Opus 4.8", "6": "Opus 4.8", "7": "Sonnet 5",
    "8": "Opus 4.8", "9": "Opus 4.8 (engine design) then Sonnet 5 (badge entries)",
    "10": "Opus 4.8 (cache strategy) then Sonnet 5 (manifest/icons)",
    "11": "Sonnet 5", "12": "Sonnet 5"
  },
  "modules": {
    "0": {
      "name": "Project Setup",
      "status": "done",
      "started": "2026-08-13",
      "completed": "2026-08-13",
      "tasks": {
        "scaffold_vite_react": "done",
        "install_core_deps": "done",
        "install_pwa_plugin": "done",
        "configure_vite_pwa": "done",
        "placeholder_icons_generated": "done",
        "build_verified": "done",
        "supabase_project_created": "done",
        "supabase_client_setup": "done",
        "env_local_created": "done",
        "supabase_connection_verified_via_curl": "done",
        "supabase_connection_verified_in_browser": "done",
        "deploy_https": "done",
        "deploy_verified_via_curl": "done",
        "pwa_installable_confirmed_on_phone": "done",
        "done_when_checklist_passed": true
      },
      "notes": [
        "Scaffolded via `npm create vite@latest . -- --template react` into a temp subfolder then moved up (create-vite's non-empty-dir prompt can't run non-interactively).",
        "Vite 8 uses the rolldown bundler; rollupOptions.manualChunks must be a function not an object, or it fails the build. Removed manualChunks entirely -- dynamic import() alone is enough to code-split Modules 5/6's heavy libs, no bundler config needed.",
        "Placeholder PNG icons (192/512, solid teal #0f9d8a) generated via a throwaway Node script (no PIL/ImageMagick available) -- swap for real branded icons before shipping.",
        "src/App.jsx replaced Vite's default template with a minimal Supabase status check using supabase.auth.getSession() (works with no tables yet -- categories/inventory_items don't exist until Module 1).",
        "package.json name fixed from 'scaffold-tmp' (leftover temp folder name) to 'expiry-tracker'.",
        "`npm run build` succeeds: service worker + manifest.webmanifest generated correctly.",
        "Supabase project created (ref rrrgwvndhubaxoxeklex); anon key verified live via /auth/v1/health -> HTTP 200.",
        "Deploy switched from Vercel-CLI plan to GitHub-connected auto-deploy: created github.com/mechahuman/Expiry-Tracker, renamed local branch master->main, pushed, connected via Vercel dashboard import with VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY env vars set there.",
        "Live at https://expiry-tracker-mu.vercel.app/ -- verified via curl (root, manifest.webmanifest, sw.js, both icons all HTTP 200) and confirmed by user: Supabase status shows 'connected' in-browser, install/Add-to-Home-Screen prompt works on phone.",
        "MODULE 0 COMPLETE 2026-08-13."
      ]
    },
    "1": {
      "name": "DB Schema (Categories, Inventory, Badges, RLS)",
      "status": "done",
      "started": "2026-08-13",
      "completed": "2026-08-13",
      "tasks": {
        "schema_sql_written": "done",
        "profiles_table": "done",
        "categories_table_seeded": "done",
        "inventory_items_table": "done",
        "badges_table": "done",
        "user_badges_table": "done",
        "rls_enabled": "done",
        "rls_policies_written": "done",
        "rls_policies_tested": "done",
        "done_when_checklist_passed": true,
        "opus_hardening_audit": "done",
        "hardening_sql_written": "done",
        "hardening_sql_run_by_user": "done",
        "hardening_verified_critical_hole_closed": "done",
        "hardening_verified_remaining": "pending_user_run_verify_hardening_sql"
      },
      "notes": [
        "I do not have Postgres/DB credentials (only VITE_SUPABASE_ANON_KEY, not the DB password or a Management API token), so schema DDL can't be run by me directly -- user ran supabase/schema.sql in the Supabase SQL Editor.",
        "supabase/schema.sql: idempotent (IF NOT EXISTS / ON CONFLICT / DROP POLICY IF EXISTS) so safe to re-run. Creates profiles, categories (seeded), inventory_items, badges, user_badges. Enables RLS + auth.uid()-based policies on profiles/inventory_items/user_badges only -- categories/badges are shared reference data, intentionally left without RLS.",
        "user_badges has no update/delete policy by design -- earned badges are append-only for the client; default-deny (no policy = no access) covers this, not an oversight.",
        "RLS verified via supabase/test_rls.sql with 2 real test auth users: impersonating test-a (id 1d238dc5-ba60-4ee6-b635-3c35485cbd63) returned only 'Test A milk'; impersonating test-b (id eacc0de3-fd86-4e45-9ff1-ace47bdf2d24) returned only 'Test B chips'. Isolation confirmed both directions. Test rows cleaned up afterward; the two test auth users/profiles were left in place (harmless, deletable later).",
        "MODULE 1 COMPLETE 2026-08-13.",
        "2026-08-19 OPUS AUDIT: found 7 issues in the original schema; fixes written to supabase/002_hardening.sql (must be run in SQL Editor after schema.sql).",
        "AUDIT-1 (CRITICAL, empirically verified): categories and badges had RLS disabled on the wrong assumption that 'reference data doesn't need RLS'. Supabase exposes all public-schema tables via PostgREST and grants anon/authenticated write privileges by default, so RLS-off = writes allowed. Proven live against this project using ONLY the anon key (public, ships in the frontend bundle): POST /rest/v1/categories -> 201, DELETE -> 204, POST /rest/v1/badges -> 201. Any visitor could rename/delete everyone's categories or badges. Fixed by enabling RLS + SELECT-only policy on both. Audit test rows were cleaned up; tables verified back to original state (categories ids 1-5, badges empty).",
        "AUDIT-2: created_at/used_at/earned_at were `timestamp` (no tz). now() is timestamptz and gets silently flattened to UTC. In an IST (UTC+5:30) date-deadline app this causes off-by-one-day errors near midnight. Converted to timestamptz. expiry_date deliberately stays `date` -- a printed best-before is a calendar date, not an instant.",
        "AUDIT-3: FKs to auth.users had no ON DELETE, defaulting to NO ACTION -- deleting a user with any inventory row would fail with an FK violation. Now ON DELETE CASCADE. category_id -> ON DELETE SET NULL (retiring a category must not delete the user's food). badge_id -> CASCADE.",
        "AUDIT-4: profiles row was to be inserted client-side after signUp() (Module 2 plan). Fragile (crash between the two calls orphans the auth user) and doesn't work for Google OAuth, which returns from redirect already authenticated with no 'just signed up' hook. Replaced with an AFTER INSERT trigger on auth.users (public.handle_new_user, security definer, search_path pinned to ''). Existing users backfilled. >>> MODULE 2 MUST NOT insert into profiles -- the trigger owns it.",
        "AUDIT-5: no indexes on the columns RLS puts in EVERY query's WHERE clause. Added (user_id,status) for Module 4's list, partial (expiry_date) where status='active' for Module 8's cron scan, and (user_id) on user_badges.",
        "AUDIT-6: bare auth.uid() in policies re-evaluates per row; wrapped in (select auth.uid()) so the planner hoists it to an InitPlan (Supabase's documented RLS perf guidance). Also made WITH CHECK explicit on UPDATE policies -- note this was NOT a hole before, Postgres falls back to the USING expression when WITH CHECK is omitted.",
        "AUDIT-7: added check (quantity > 0). Module 3's React Hook Form validation is UX, not enforcement -- the REST endpoint is public.",
        "AUDIT-DEFERRED (design decision, revisit at Module 9): profiles.points and user_badges remain directly writable by their owner, so a user can set their own points or award themselves badges from devtools, bypassing the client-side criteria logic. Fix if wanted: revoke update(points) from authenticated + move awards into SECURITY DEFINER functions. Left as-is for now since Module 9's reward logic doesn't exist yet and this is a trust-the-user college project."
      ]
    },
    "2": {
      "name": "Auth & Onboarding",
      "status": "done",
      "started": "2026-08-19",
      "completed": "2026-08-20",
      "decisions": {
        "auth_methods": "Email/password only. Google OAuth DEFERRED to a later standalone pass (needs a Google Cloud project + consent screen + redirect URIs from the user). Build the login screen so a Google button can slot in later without rework.",
        "routing": "react-router-dom (to install). Real URLs are needed to make the 'logged-out users cannot reach Home by URL' checklist item genuinely testable, and Modules 3-9 each need their own screen anyway.",
        "email_confirmation": "OFF during development (Supabase Dashboard -> Authentication -> Providers -> Email -> disable 'Confirm email'). MUST be re-enabled before real users -- see launch_checklist."
      },
      "tasks": {
        "install_react_router": "done",
        "onboarding_carousel": "done",
        "login_signup_screen": "done",
        "google_oauth": "deferred_by_decision",
        "auth_state_listener_zustand": "done",
        "route_protection": "done",
        "home_empty_state": "done",
        "vercel_spa_rewrite": "done",
        "lint_and_build_pass": "done",
        "browser_tested": "done -- user disabled email confirmation, confirmed working signup/login in browser (screenshot: logged in as test-b, Home showing Points: 0)",
        "done_when_checklist_passed": true
      },
      "files_added": [
        "src/store/authStore.js -- Zustand: session, loading, hasOnboarded (localStorage-backed), setSession, completeOnboarding, signOut",
        "src/components/ProtectedRoute.jsx -- gates /home; waits on `loading` before redirecting",
        "src/pages/Onboarding.jsx + .css -- 3 slides, CSS scroll-snap, no library",
        "src/pages/Login.jsx + .css -- React Hook Form, login/signup mode toggle",
        "src/pages/Home.jsx + .css -- empty state, logout, reads own profiles row",
        "src/App.jsx -- BrowserRouter, routes, auth bootstrap (getSession + onAuthStateChange)",
        "src/index.css -- replaced Vite demo styles with mobile-first tokens (teal #0f9d8a matching manifest)",
        "vercel.json -- SPA rewrite; without it /login and /home 404 on Vercel",
        "DELETED src/App.css (Vite template leftover)"
      ],
      "notes": [
        "DO NOT insert into profiles on signup. The on_auth_user_created trigger (added in 002_hardening.sql) creates the profile row atomically for every signup path. A client-side insert would be redundant and would fail on PK conflict. This overrides the original roadmap's Module 2 step 3.",
        "The `loading` flag in authStore is load-bearing: Supabase restores sessions asynchronously, so ProtectedRoute must wait for it or refreshing on /home bounces a logged-in user to /login.",
        "vercel.json SPA rewrite is required for BrowserRouter -- direct navigation to /login or /home would otherwise 404 on Vercel (Vite's dev server has this fallback built in, so the bug only shows up in production).",
        "Login.jsx handles the no-session-returned case (email confirmation enabled) by showing a 'check your inbox' notice, so re-enabling confirmation for launch won't silently break the screen.",
        "BLOCKER FOUND 2026-08-19 via live API test: email confirmation is STILL ENABLED. POST /auth/v1/signup created the auth user but returned confirmation_sent_at and no access_token; the follow-up password login then returned HTTP 400. User must disable it: Supabase -> Authentication -> Providers -> Email -> turn off 'Confirm email'.",
        "Note: Supabase rejects @example.com addresses (email_address_invalid) -- use a real-looking domain when testing signup via the API.",
        "Test auth users created during testing that should be cleaned up later: m2test1787142475@fastmail.com (id 924ab176-5658-4375-84e6-b96046ea8f75), plus the Module 1 test-a/test-b users.",
        "MODULE 2 COMPLETE 2026-08-20. NOTE: test-a@example.com's actual password is NOT 'TestPass123!' (that was only ever a suggested example) -- user hit 'Invalid login credentials' with it. test-b@example.com's real password IS whatever the user set and used successfully. Don't assume either password in future sessions -- use the signup API to mint a fresh known-password test user instead (see Module 3 verification approach)."
      ]
    },
    "3": {
      "name": "Manual Entry",
      "status": "done",
      "started": "2026-08-20",
      "completed": "2026-08-20",
      "tasks": {
        "item_form_component_built": "done",
        "add_item_route": "done",
        "home_button_wired": "done",
        "home_flash_banner": "done",
        "select_styling_added": "done",
        "shared_field_form_banner_moved_to_index_css": "done",
        "lint_and_build_pass": "done",
        "api_level_verification": "done",
        "browser_tested_by_user": "pending_user"
      },
      "decisions": {
        "reusable_form": "Built src/components/ItemForm.jsx as a standalone component (not inlined into AddItem.jsx) because Module 7 (Verify Details, not yet built) explicitly reuses 'the exact form fields from Module 3' to let Voice/OCR pre-fill it. Props: initialValues (partial, for Module 7's pre-fill), inputMethod (defaults 'manual'), onSaved(item) callback. The component is deliberately ignorant of navigation -- callers decide what happens after save.",
        "cancel_semantics_belong_to_the_page": "AddItem.jsx owns its own Cancel button rather than ItemForm having one, because Module 7's cancel behavior differs ('discard draft, return to capture screen') -- keeping that logic in the page shell avoids ItemForm needing a mode-dependent cancel prop later."
      },
      "files_added": [
        "src/components/ItemForm.jsx + .css -- reusable form (name/quantity/unit/category/expiry_date), fetches categories on mount, validates quantity>0 client-side to match the DB check constraint, soft-warns (doesn't block) on a past expiry_date",
        "src/pages/AddItem.jsx + .css -- page shell at /add, Cancel button, renders ItemForm with inputMethod='manual'"
      ],
      "files_changed": [
        "src/App.jsx -- added /add route behind ProtectedRoute",
        "src/pages/Home.jsx -- 'Add your first item' button now navigates to /add (was disabled); reads location.state.flash and shows a 3s success banner after a save, then clears it from history state so it doesn't reappear on back/refresh",
        "src/pages/Home.css -- removed unused .coming-soon, added .home-flash",
        "src/index.css -- select now styled identically to input; MOVED .field/.field>span/.form-banner(+.error/.notice) here from Login.css since ItemForm needed them too -- relying on Login.jsx's import order to make them globally available was fragile (would break if pages later become React.lazy code-split)",
        "src/pages/Login.css -- removed the classes moved to index.css"
      ],
      "notes": [
        "Verified end-to-end via the REST API using a freshly-signed-up test user (m3test<timestamp>@fastmail.com, password TestPass123!) rather than guessing test-a/test-b's passwords:",
        "  1. Valid insert (with correct user_id, quantity=2, unit=l, input_method=manual) -> HTTP 201, row returned correctly.",
        "  2. quantity=0 -> HTTP 400, rejected by check constraint inventory_items_quantity_positive (confirms client validation matches a real DB-level guard, not just UX).",
        "  3. input_method='typed' (not in manual/voice/ocr) -> HTTP 400, rejected by inventory_items_input_method_check.",
        "  4. Insert claiming a different user's UUID as user_id -> HTTP 403, rejected by RLS (42501) -- confirms Module 1's RLS still holds under a real write from Module 3's exact insert shape.",
        "  5. Read-back showed only this user's own row. Test row deleted afterward (HTTP 204), table confirmed empty again.",
        "This test run created one more throwaway auth user (m3test...@fastmail.com) -- add to the batch cleanup of test-a/test-b/m2test/m3test users later.",
        "npm run lint and npm run build both pass clean.",
        "Browser UI test (button click -> form -> submit -> flash banner -> past-date warning) NOT YET run by the user -- the API test proves the data layer is correct but not that the React form itself renders/behaves right.",
        "MODULE 3 COMPLETE 2026-08-20 -- user confirmed 'working perfectly' after testing on the deployed Vercel URL."
      ]
    },
    "4": {
      "name": "Inventory List & Mark as Used",
      "status": "done",
      "started": "2026-08-20",
      "completed": "2026-08-20",
      "tasks": {
        "fetch_active_items_scoped_to_user": "done",
        "days_left_color_coding": "done",
        "filter_chips_all_soon_expired_category": "done",
        "search_bar": "done",
        "mark_as_used": "done",
        "empty_state_vs_no_matches_state": "done",
        "check_badge_progress_stub": "done",
        "lint_and_build_pass": "done",
        "api_level_verification": "done",
        "browser_tested_by_user": "pending_user"
      },
      "decisions": {
        "list_lives_on_home_not_a_new_route": "Inventory list is the main screen -- built into Home.jsx rather than a separate /inventory route, since Home already owned the empty-state placeholder Module 3 left as a stub.",
        "category_join_not_separate_fetch": "Uses Supabase's embedded-resource select (`category:categories(name)`) via the category_id FK rather than a second query, satisfying the roadmap's 'no extra network calls for filters' requirement by not needing one in the first place.",
        "extracted_shared_date_util": "Pulled todayISO() out of ItemForm.jsx into src/lib/date.js (also added daysUntil()) since Home now needs the same local-date logic -- avoids the two files drifting out of sync on a timezone edge case.",
        "checkBadgeProgress_stub_added_at_both_call_sites": "src/lib/badges.js: a documented no-op, called (fire-and-forget) from both AddItem.jsx's onSaved and Home.jsx's handleMarkUsed, matching the roadmap's Module 9 hook points -- Module 9 only has to fill in the function body, not go find where to call it."
      },
      "files_added": [
        "src/lib/date.js -- todayISO(), daysUntil() (ISO date-string math, timezone-safe by construction -- see file comment)",
        "src/lib/badges.js -- checkBadgeProgress(userId) no-op stub for Module 9",
        "src/components/ItemCard.jsx + .css -- one item row: name/qty/unit/category, color-coded expiry badge (red <=2d, orange <=7d, teal beyond), Mark as used button"
      ],
      "files_changed": [
        "src/pages/Home.jsx -- full rewrite: fetches active items scoped to user_id+status (matches the (user_id,status) index from 002_hardening.sql), search + filter-chip state (useMemo, purely client-side against the already-fetched list), mark-as-used (optimistic local removal, not a refetch), distinguishes 'loading' vs 'zero items ever' vs 'zero items match the current filter'",
        "src/pages/AddItem.jsx -- calls checkBadgeProgress stub in onSaved",
        "src/components/ItemForm.jsx -- now imports todayISO from src/lib/date.js instead of a local copy",
        "src/index.css -- added --warning/--warning-bg tokens (light+dark) for the 'soon' expiry badge; red and teal already existed, orange didn't",
        "src/pages/Home.css -- added search input container, filter chip styles, item list, loading/no-matches states, floating add button"
      ],
      "notes": [
        "Verified end-to-end via the REST API with a fresh test user (m4test<timestamp>@fastmail.com): seeded 4 items spanning the expiry thresholds (expired -3d, urgent +1d, soon +5d, ok +60d, across 3 different categories), ran the EXACT query shape Home.jsx uses (select with category join, user_id+status filter, order by expiry_date) and confirmed correct rows/ordering/category names came back.",
        "Verified mark-as-used: PATCH status='used'+used_at succeeds (204), item disappears from the active-filtered query but the row itself is preserved (not deleted) with status='used'.",
        "Verified an update attempt using only the anon key (no real user session) affects 0 rows (RLS silently filters it out rather than erroring) -- first time Module 4 has actually exercised UPDATE under RLS; only INSERT and SELECT had been tested before this.",
        "All seeded test data cleaned up afterward, confirmed empty.",
        "npm run lint and npm run build both pass clean.",
        "Browser UI test NOT YET run by user -- filter chip interaction, search-as-you-type, and the color thresholds actually rendering correctly still need eyes on a real screen."
      ]
    }
  },
  "pending_user_actions": [
    "Browser-test Module 4: add 2-3 items with different expiry dates/categories via the (now working) Add screen, then confirm on Home: color-coded badges look right, filter chips (All/Expiring soon/Expired/category) actually filter, search box filters as you type, 'Mark as used' removes an item from the list.",
    "Optional: run supabase/verify_hardening.sql in the SQL Editor to confirm the non-API-visible parts of 002 landed (timestamptz, FK cascades, signup trigger, indexes, quantity check). The critical RLS fix is already verified independently."
  ],
  "key_config": {
    "supabase_url": "https://rrrgwvndhubaxoxeklex.supabase.co",
    "supabase_anon_key": "set in .env.local (gitignored) -- verified live via /auth/v1/health returning HTTP 200",
    "deploy_target": "Vercel (via GitHub-connected auto-deploy)",
    "deploy_url": "https://expiry-tracker-mu.vercel.app/",
    "vercel_project_dashboard": "https://vercel.com/manav-bhavsars-projects-ae85607f/expiry-tracker",
    "git_initialized": true,
    "initial_commit": "7e72f76",
    "github_repo": "https://github.com/mechahuman/Expiry-Tracker",
    "default_branch": "main"
  },
  "launch_checklist": [
    "Re-enable Supabase email confirmation (Auth -> Providers -> Email -> 'Confirm email') -- disabled during development for faster test loops.",
    "Replace the placeholder solid-teal PWA icons in public/ (icon-192.png, icon-512.png) with real branded artwork.",
    "Delete throwaway test auth users from Authentication -> Users: test-a@example.com, test-b@example.com, m2test1787142475@fastmail.com, m3test<timestamp>@fastmail.com, m4test<timestamp>@fastmail.com.",
    "Decide on gamification integrity: profiles.points and user_badges are currently writable by their owner, so points/badges can be self-awarded from devtools. See Module 1 AUDIT-DEFERRED."
  ],
  "last_updated": "2026-08-20"
}
